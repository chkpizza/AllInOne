<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AllInOne" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!--
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="colorSecondaryContainer">@color/white</item>
        -->
    </style>

    <style name="Theme.AllInOne" parent="Base.Theme.AllInOne">
        <item name="bottomSheetDialogTheme">@style/DefaultBottomSheetDialog</item>
        <item name="android:navigationBarColor">#F6F6F6</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>