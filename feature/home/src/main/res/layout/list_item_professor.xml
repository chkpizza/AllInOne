<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />
        <variable
            name="item"
            type="com.wantique.home.domain.model.ProfessorItem" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:paddingTop="4dp"
        android:layout_marginStart="6dp"
        android:paddingStart="2dp"
        android:layout_marginEnd="6dp"
        android:paddingEnd="2dp">

        <TextView
            android:id="@+id/home_tv_professor_belong"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            app:belong="@{item}"
            style="@style/titleTextViewStyle"
            android:background="@drawable/background_professor"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <TextView
            android:id="@+id/home_tv_professor_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            style="@style/defaultTextViewStyle"
            app:name="@{item}"
            app:layout_constraintTop_toBottomOf="@id/home_tv_professor_belong"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="이선재 교수님" />

        <TextView
            android:id="@+id/home_tv_professor_shadow"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            style="@style/maskTextViewStyle"
            android:text="@string/home_professor_not_update"
            android:visibility="@{item.update != true ? View.VISIBLE : View.GONE}"
            android:gravity="center" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>